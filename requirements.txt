# Production Dependencies
Django>=3.0,<4.0
# Pin 1.14.x: Crispy 2+ moved Bootstrap 4 templates out of core; 1.14 still bundles bootstrap4/ (needed for create/edit event, etc.).
django-crispy-forms==1.14.0
# 2.1+ required for Django 4+ (gettext_lazy / removed ugettext_lazy).
django-mapbox-location-field>=2.1.0
# 6.7+ required for Django 4+ (force_str / removed force_text).
django-ckeditor>=6.7.0
django-betterforms>=1.2
six>=1.16.0  # django-betterforms 1.2 still imports django.utils.six (removed in Django 3+); we alias to this

# Production database drivers
psycopg2-binary>=2.8.5  # PostgreSQL
mysqlclient>=2.0.0     # MySQL

# Production server
gunicorn>=20.0.4

# Environment variables
python-dotenv>=0.19.0

# CAPTCHA (password reset & other forms). Use 0.5.x with Django 3; upgrade to 0.6+ when on Django 4.2+.
django-simple-captcha>=0.5.20,<0.6.0

# i18n: compile .po to .mo without gettext (Windows)
polib>=1.2.0

# Image handling
Pillow>=8.0.0
qrcode>=7.0  # Gate student QR images

# Load testing (optional, for pre-deployment tests)
requests>=2.25.0

# Excel export
openpyxl>=3.0.0

# PDF export (admin reports)
reportlab>=3.6.0

# HTML-to-PDF (visitor e-ID card download — exact layout as modal)
weasyprint>=60.0

# PDF via browser (Windows fallback when WeasyPrint GTK libs missing)
playwright>=1.40.0
